export const USERSTATUES = Object.freeze({
  TRUE: true,
  FALSE: false,
});

export const STATUSES = Object.freeze({
  IDLE: 'idle',
  LOADING: 'loading',
  ERROR: 'error',
});

export const HTTP_STATUSES = Object.freeze({
  SUCCESS: 200,
});
